import numpy as np
import matplotlib.pyplot as plt
from fracdiff import fracdiff


t = np.linspace(0, 3, 1000)
x = t
for alpha in np.arange(0, 2, 0.2):
    y = fracdiff(x, alpha, t)
    #plt.plot(t, y)
#plt.show()
